<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.utc.blog</groupId>
    <artifactId>log-sdk</artifactId>
    <version>1.0.0</version>
    <packaging>jar</packaging>

    <name>log-sdk</name>
    <description>Client gửi log về admin-me (POST /api/log)</description>

    <properties>
        <maven.compiler.release>11</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <junit.version>5.10.2</junit.version>
        <logback.version>1.5.18</logback.version>
    </properties>

    <!-- Không có dependency runtime: chỉ dùng java.net.http của JDK,
         tránh đụng version Jackson/HttpClient của service tích hợp. -->
    <dependencies>
        <!-- Chỉ cần khi dùng AdminLogAppender; scope provided nên service nào không xài
             Logback thì SDK vẫn sạch dependency. -->
        <dependency>
            <groupId>ch.qos.logback</groupId>
            <artifactId>logback-classic</artifactId>
            <version>${logback.version}</version>
            <scope>provided</scope>
        </dependency>

        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>${junit.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <!-- Xuat ban len GitHub Packages cua chinh repo nay: mvn deploy -->
    <distributionManagement>
        <repository>
            <id>github</id>
            <name>GitHub Packages - log-internal</name>
            <url>https://maven.pkg.github.com/ToThangGTVT/log-internal</url>
        </repository>
    </distributionManagement>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.2.5</version>
            </plugin>
        </plugins>
    </build>
</project>
